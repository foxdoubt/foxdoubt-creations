import * as React from "react";
import { PageProps } from "gatsby";

// wordCount={wordCount} readTime={readTime} author={author}
export default ({ wordCount, readTime, author }) => {
  console.log({ wordCount, readTime, author });
  return <></>;
};
